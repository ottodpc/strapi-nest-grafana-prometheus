config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 100
scenarios:
  - name: "nestjs-server"
    flow:
      - get:
          url: "/"
      # - get:
      #     url: "/metrics"
